# Kubernetes

Kubernetes manifests will live here when this repo grows beyond Docker Swarm.

Use `base/` for reusable service definitions and `overlays/` for cluster-specific
settings such as Ethernet versus QSFP fabric, node labels, storage classes, and
GPU scheduling details.
