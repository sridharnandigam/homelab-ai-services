# Kubernetes Base

Reusable Kubernetes manifests go here.

Keep base manifests free of cluster-specific assumptions. Put DGX Spark node
labels, storage classes, host paths, and networking choices in overlays.
