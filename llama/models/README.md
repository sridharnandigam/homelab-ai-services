# Models

Local llama.cpp model files can be placed here for development.

Do not commit model files. Cluster deployments should use a consistent external
path such as `/mnt/models` on every node or shared storage mounted there.
